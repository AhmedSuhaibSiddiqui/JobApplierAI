# JobApplierAI Agent - Backend 🧠

The backend acts as the central intelligence unit for the JobApplierAI Agent. It exposes a **FastAPI** service that orchestrates AI analysis, file generation, and browser automation.

## 🛠️ Tech Stack

*   **Framework:** FastAPI (Python 3.10+)
*   **AI Engine:** Google Gemini 2.5 Flash (via `google-generativeai`)
*   **Automation:** Playwright (Chromium)
*   **Document Processing:** `xhtml2pdf` (PDF Generation), `markdown`

## ⚙️ Configuration

1.  **Environment Variables:**
    Create a `.env` file in this directory:
    ```ini
    GEMINI_API_KEY=your_google_ai_studio_key_here
    ```

2.  **Dependencies:**
    ```bash
    pip install -r requirements.txt
    playwright install chromium
    ```

## 📂 Key Components

| File | Purpose |
|------|---------|
| `main.py` | The API Entry point. Defines routes for `/analyze`, `/generate-pdf`, and `/apply`. |
| `ai_agent.py` | Wraps the Gemini API. Handles prompts for "Career Coaching" and "Resume Tailoring". |
| `browser_agent.py` | The Playwright script. Detects job board type (Greenhouse/Lever) and fills forms. |
| `resume_renderer.py` | Converts the Markdown text generated by AI into a CSS-styled PDF file. |

## 🚀 Running the Server

```bash
python main.py
```
The server will start at `http://localhost:8000`.
Docs are available at `http://localhost:8000/docs`.
